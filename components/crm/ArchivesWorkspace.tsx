"use client";
import Link from "next/link";
import { useState } from "react";
import type { CrmLead } from "@/lib/crm/types";
export function ArchivesWorkspace({ initialLeads }: { initialLeads: CrmLead[] }) { const [leads,setLeads]=useState(initialLeads); async function restore(id:string){const response=await fetch(`/api/crm/leads/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({archived_at:null})});if(response.ok)setLeads((current)=>current.filter((lead)=>lead.id!==id));} return <div className="p-5 lg:p-8"><div className="overflow-hidden rounded-xl border border-[#E8E4DB] bg-white"><table className="w-full min-w-[720px] text-left text-xs"><thead className="bg-[#FAF9F7] text-[10px] uppercase text-[#8B7AA8]"><tr>{["Nom","Ville","Métier","Statut","Archivé le",""] .map((label)=><th key={label} className="p-3">{label}</th>)}</tr></thead><tbody>{leads.map((lead)=><tr key={lead.id} className="border-t border-[#E8E4DB]"><td className="p-3"><Link href={`/crm/leads/${lead.id}`} className="font-black">{lead.name}</Link></td><td className="p-3">{lead.city??"—"}</td><td className="p-3">{lead.business_type??"—"}</td><td className="p-3">{lead.commercial_status}</td><td className="p-3">{lead.archived_at?new Date(lead.archived_at).toLocaleDateString("fr-FR"):"—"}</td><td className="p-3 text-right"><button onClick={()=>void restore(lead.id)} className="font-black text-[#4C1D95]">Restaurer</button></td></tr>)}</tbody></table>{!leads.length?<div className="p-10 text-center text-sm font-semibold text-[#8B7AA8]">Aucun lead archivé.</div>:null}</div></div>; }
