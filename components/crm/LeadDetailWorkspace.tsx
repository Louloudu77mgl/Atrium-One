"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useMemo, useState } from "react";
import { calculateArr } from "@/lib/crm/logic";
import { COMMERCIAL_STATUSES, CRM_CALL_RESULTS, CRM_EVENT_TYPES, CRM_MODULES, CRM_OPPORTUNITY_STATUSES, MODULE_LABELS, type BusinessAccess, type CrmEvent, type CrmLead, type CrmOpportunity, type CrmTask } from "@/lib/crm/types";

type Note = { id: string; content: string; created_at: string; updated_at: string; created_by: string | null };
type Activity = { id: string; type: string; metadata: Record<string, unknown>; created_at: string };
type Candidate = { businessId: string; userId: string; businessName: string; email: string | null; reason: string };
type Account = { email: string | null; createdAt: string | null; lastSignInAt: string | null } | null;
type Tab = "summary" | "notes" | "tasks" | "events" | "opportunities" | "access";
const formatDateTime = (value?: string | null) => value ? new Date(value).toLocaleString("fr-FR", { dateStyle: "medium", timeStyle: "short" }) : "Non disponible";
const tabs: Array<[Tab, string]> = [["summary", "Résumé"], ["notes", "Notes"], ["tasks", "Tâches"], ["events", "Événements"], ["opportunities", "Opportunités"], ["access", "Accès AtriumOne"]];

export function LeadDetailWorkspace({ initial, returnTo }: { initial: { lead: CrmLead; notes: Note[]; tasks: CrmTask[]; events: CrmEvent[]; opportunities: CrmOpportunity[]; activity: Activity[]; access: BusinessAccess | null; modules: Array<{ module_key: string; enabled: boolean }>; account: Account; candidates: Candidate[] }; returnTo?: string }) {
  const router = useRouter();
  const [tab, setTab] = useState<Tab>("summary");
  const [lead, setLead] = useState(initial.lead);
  const [notes, setNotes] = useState(initial.notes);
  const [tasks, setTasks] = useState(initial.tasks);
  const [events, setEvents] = useState(initial.events);
  const [opportunities, setOpportunities] = useState(initial.opportunities);
  const [activity, setActivity] = useState(initial.activity);
  const [editingIdentity, setEditingIdentity] = useState(false);
  const [showTask, setShowTask] = useState(false);
  const [showEvent, setShowEvent] = useState(false);
  const [showOpportunity, setShowOpportunity] = useState(false);
  const [eventType, setEventType] = useState<(typeof CRM_EVENT_TYPES)[number]>("Appel effectué");
  const [opportunityMrr, setOpportunityMrr] = useState(0);
  const [message, setMessage] = useState<string | null>(null);
  const [accountEnabled, setAccountEnabled] = useState(initial.access?.account_enabled ?? false);
  const [onboardingStatus, setOnboardingStatus] = useState(initial.access?.onboarding_status ?? "pending");
  const [modules, setModules] = useState<Record<string, boolean>>(Object.fromEntries(CRM_MODULES.map((key) => [key, initial.modules.find((row) => row.module_key === key)?.enabled ?? false])));
  const nextEvent = useMemo(() => events.filter((item) => item.event_time && `${item.event_date}T${item.event_time}` >= new Date().toISOString().slice(0, 19)).sort((a,b) => `${a.event_date}${a.event_time}`.localeCompare(`${b.event_date}${b.event_time}`))[0] ?? null, [events]);

  function addLocalActivity(type: string, metadata: Record<string, unknown>) { setActivity((current) => [{ id: crypto.randomUUID(), type, metadata, created_at: new Date().toISOString() }, ...current]); }
  async function patchLead(update: Record<string, unknown>) { const response = await fetch(`/api/crm/leads/${lead.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(update) }); const data = await response.json(); if (!response.ok) return setMessage(data.error?.message ?? "Modification impossible."); setLead((current) => ({ ...current, ...data })); setMessage("Modifications enregistrées."); }
  async function saveIdentity(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); await patchLead(Object.fromEntries(new FormData(event.currentTarget).entries())); setEditingIdentity(false); }

  async function addNote(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const content = String(new FormData(form).get("content") ?? ""); const response = await fetch(`/api/crm/leads/${lead.id}/notes`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ content }) }); if (response.ok) { const created = await response.json(); setNotes((current) => [created, ...current]); addLocalActivity("note_added", {}); form.reset(); } }
  async function editNote(note: Note) { const content = prompt("Modifier la note", note.content); if (!content) return; const response = await fetch(`/api/crm/notes/${note.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ content }) }); if (response.ok) { const data = await response.json(); setNotes((current) => current.map((item) => item.id === note.id ? data : item)); } }
  async function deleteNote(id: string) { if (confirm("Supprimer cette note ?") && (await fetch(`/api/crm/notes/${id}`, { method: "DELETE" })).ok) setNotes((current) => current.filter((item) => item.id !== id)); }

  async function addTask(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const body = Object.fromEntries(new FormData(form).entries()); const response = await fetch(`/api/crm/leads/${lead.id}/tasks`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); if (response.ok) { const created = await response.json(); setTasks((current) => [...current, created]); addLocalActivity("task_created", { title: created.title }); setShowTask(false); form.reset(); } }
  async function updateTask(id: string, update: Record<string, unknown>) { const response = await fetch(`/api/crm/tasks/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(update) }); if (response.ok) { const data = await response.json(); setTasks((current) => current.map((item) => item.id === id ? data : item)); if ("completed" in update) addLocalActivity(update.completed ? "task_completed" : "task_updated", { title: data.title }); } }
  async function editTask(task: CrmTask) { const due_date = prompt("Date (AAAA-MM-JJ)", task.due_date); if (!due_date) return; const due_time = prompt("Heure facultative (HH:MM)", task.due_time?.slice(0,5) ?? ""); if (due_time === null) return; const description = prompt("Description facultative", task.description ?? ""); if (description === null) return; await updateTask(task.id, { due_date, due_time, description }); }
  async function deleteTask(id: string) { if (confirm("Supprimer cette tâche ?") && (await fetch(`/api/crm/tasks/${id}`, { method: "DELETE" })).ok) setTasks((current) => current.filter((item) => item.id !== id)); }

  async function addEvent(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const body = Object.fromEntries(new FormData(form).entries()); const response = await fetch(`/api/crm/leads/${lead.id}/events`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const data = await response.json(); if (response.ok) { setEvents((current) => [data, ...current]); addLocalActivity(eventActivityType(data.type), { title: data.title, type: data.type, result: data.call_result ?? data.result }); setShowEvent(false); form.reset(); } else setMessage(data.error?.message ?? "Événement impossible."); }
  async function editEvent(item: CrmEvent) { const notes = prompt("Notes", item.notes ?? ""); if (notes === null) return; const result = prompt("Résultat", item.result ?? item.call_result ?? ""); if (result === null) return; const response = await fetch(`/api/crm/events/${item.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(item.type === "Appel effectué" ? { notes, call_result: CRM_CALL_RESULTS.includes(result as never) ? result : item.call_result } : { notes, result }) }); if (response.ok) { const data = await response.json(); setEvents((current) => current.map((currentItem) => currentItem.id === item.id ? data : currentItem)); } }
  async function deleteEvent(id: string) { if (confirm("Supprimer cet événement ?") && (await fetch(`/api/crm/events/${id}`, { method: "DELETE" })).ok) setEvents((current) => current.filter((item) => item.id !== id)); }

  async function addOpportunity(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const body = Object.fromEntries(new FormData(form).entries()); const response = await fetch(`/api/crm/leads/${lead.id}/opportunities`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const data = await response.json(); if (response.ok) { setOpportunities((current) => [data, ...current]); addLocalActivity("opportunity_created", { name: data.name, mrr: data.mrr, arr: data.arr }); setShowOpportunity(false); setOpportunityMrr(0); form.reset(); } }
  async function updateOpportunity(item: CrmOpportunity, status: string) {
    let lostReason: string | null = null;
    if (status === "Gagnée" && !confirm(`Affaire closée ?\n\n${item.name} passera en Gagnée et ${lead.name} deviendra Client.`)) return;
    if (status === "Perdue") { lostReason = prompt("Raison de la perte", item.lost_reason ?? ""); if (lostReason === null) return; }
    const response = await fetch(`/api/crm/opportunities/${item.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status, lost_reason: lostReason }) });
    const data = await response.json();
    if (response.ok) { setOpportunities((current) => current.map((currentItem) => currentItem.id === item.id ? data : currentItem)); if (status === "Gagnée") { setLead((current) => ({ ...current, commercial_status: "Client", mrr: data.mrr, monthly_value: data.mrr })); addLocalActivity("deal_won", { name: data.name, mrr: data.mrr, arr: data.arr }); addLocalActivity("prospect_became_client", {}); } else addLocalActivity(status === "Perdue" ? "deal_lost" : "opportunity_status_changed", { name: data.name, to: status }); }
  }
  async function deleteOpportunity(id: string) { if (confirm("Supprimer cette opportunité ?") && (await fetch(`/api/crm/opportunities/${id}`, { method: "DELETE" })).ok) setOpportunities((current) => current.filter((item) => item.id !== id)); }

  async function saveAccess(businessId?: string) { if (!lead.business_id && !businessId) return; if (!initial.access?.account_enabled && accountEnabled && !confirm("Activer AtriumOne pour ce client ?")) return; const manual = Boolean(businessId); if (manual && !confirm("Associer ce compte AtriumOne à ce prospect ?")) return; const response = await fetch(`/api/crm/leads/${lead.id}/access`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ businessId, manualConfirmed: manual, accountEnabled, onboardingStatus, modules }) }); const data = await response.json(); if (!response.ok) return setMessage(data.error?.message ?? "Accès non enregistrés."); if (data.businessId && !lead.business_id) setLead((current) => ({ ...current, business_id: data.businessId })); setAccountEnabled(data.accountEnabled); setOnboardingStatus(data.onboardingStatus); setModules(data.modules); setMessage("Accès AtriumOne enregistrés."); router.refresh(); }

  async function deleteLead(permanent = false) {
    const warning = lead.business_id ? "\n\nLe compte AtriumOne associé sera conservé." : "";
    if (!confirm(`${permanent ? "Supprimer définitivement" : "Supprimer"} ce lead ?${warning}`)) return;
    const suffix = permanent ? `?permanent=1${lead.business_id ? "&confirm_account_preserved=1" : ""}` : "";
    const response = await fetch(`/api/crm/leads/${lead.id}${suffix}`, { method: "DELETE" });
    if (response.ok) router.push("/crm/leads");
  }

  return <div>
    <header className="border-b border-[#E8E4DB] bg-white px-5 py-5 lg:px-8"><div className="mb-3"><Link href={returnTo ?? "/crm/leads"} className="text-xs font-black text-[#6B617F]">← {returnTo ? "Retour à la journée" : "Base de données"}</Link></div><div className="flex flex-wrap items-end justify-between gap-4"><div><div className="flex items-center gap-2"><h1 className="text-2xl font-black tracking-[-.03em]">{lead.name}</h1><span className="rounded-full bg-[#F3E8FF] px-2.5 py-1 text-[10px] font-black text-[#4C1D95]">{lead.lead_source}</span></div><p className="mt-1 text-sm font-semibold text-[#6B617F]">{lead.business_type ?? "Métier non renseigné"} · {lead.city ?? "Ville inconnue"}</p></div><div className="flex flex-wrap gap-2"><select value={lead.commercial_status} onChange={(event) => void patchLead({ commercial_status: event.target.value })} className="ao-select h-10 px-3 text-xs font-black">{COMMERCIAL_STATUSES.map((status) => <option key={status}>{status}</option>)}</select><button onClick={() => void patchLead({ archived_at: new Date().toISOString() }).then(() => router.push("/crm/leads"))} className="ao-btn-secondary px-3 text-xs font-black">Archiver</button><button onClick={() => void deleteLead()} className="ao-btn-secondary px-3 text-xs font-black text-red-600">Supprimer</button><button onClick={() => void deleteLead(true)} className="rounded-lg px-2 text-[10px] font-black text-red-700">Définitif</button></div></div>{message ? <div className="mt-3 rounded-lg bg-[#F3E8FF] px-3 py-2 text-xs font-bold text-[#4C1D95]">{message}</div> : null}</header>

    <nav className="sticky top-0 z-20 flex gap-1 overflow-x-auto border-b border-[#E8E4DB] bg-[#FAF9F7]/95 px-5 py-2 backdrop-blur lg:px-8">{tabs.map(([key, label]) => <button key={key} onClick={() => setTab(key)} className={`whitespace-nowrap rounded-lg px-3 py-2 text-xs font-black ${tab === key ? "bg-[#2E1065] text-white" : "text-[#6B617F] hover:bg-white"}`}>{label}</button>)}</nav>

    <main className="p-5 lg:p-8">
      {tab === "summary" ? <div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_420px]">
        <div className="space-y-4"><Section title="Identité" action={<button onClick={() => setEditingIdentity(!editingIdentity)} className="text-xs font-black text-[#4C1D95]">{editingIdentity ? "Annuler" : "Modifier"}</button>}>{editingIdentity ? <form onSubmit={saveIdentity} className="grid gap-3 sm:grid-cols-2">{[["name","Nom"],["business_type","Métier"],["address","Adresse"],["city","Ville"],["postal_code","Code postal"],["phone","Téléphone"],["email","Email"],["website","Site"]].map(([key,label]) => <label key={key} className={`ao-label ${key === "address" ? "sm:col-span-2" : ""}`}>{label}<input name={key} defaultValue={String(lead[key as keyof CrmLead] ?? "")} className="ao-input h-9 px-2.5" /></label>)}<button className="ao-btn-primary h-9 px-3 text-xs font-black sm:col-span-2">Enregistrer</button></form> : <div className="grid gap-x-5 gap-y-3 sm:grid-cols-2"><Fact label="Métier" value={lead.business_type} /><Fact label="Adresse" value={[lead.address, lead.postal_code, lead.city].filter(Boolean).join(", ")} /><Fact label="Téléphone" value={lead.phone} href={lead.phone ? `tel:${lead.phone}` : undefined} /><Fact label="Email" value={lead.email} href={lead.email ? `mailto:${lead.email}` : undefined} /><Fact label="Site" value={lead.website} href={lead.website ?? undefined} /><Fact label="Google Maps" value={lead.google_maps_url ? "Ouvrir la fiche" : null} href={lead.google_maps_url ?? undefined} /><Fact label="Note Google" value={lead.google_rating ? `${lead.google_rating} / 5` : null} /><Fact label="Nombre d’avis" value={lead.google_reviews_count?.toString()} /><Fact label="Ajouté le" value={formatDateTime(lead.created_at)} /></div>}</Section>
          {nextEvent ? <div className="rounded-xl border border-fuchsia-300 bg-gradient-to-r from-violet-600 to-fuchsia-500 p-4 text-white shadow-lg"><div className="text-[10px] font-black uppercase tracking-wider text-white/70">Prochain événement</div><div className="mt-1 text-base font-black">{nextEvent.title}</div><div className="mt-1 text-xs font-bold text-white/80">{new Date(`${nextEvent.event_date}T${nextEvent.event_time}`).toLocaleString("fr-FR", { dateStyle: "full", timeStyle: "short" })}{nextEvent.duration_minutes ? ` · ${nextEvent.duration_minutes} min` : ""}</div></div> : null}
        </div>
        <Section title="Timeline"><Timeline activity={activity} /></Section>
      </div> : null}

      {tab === "notes" ? <Section title="Notes"><form onSubmit={addNote} className="flex gap-2"><textarea name="content" required placeholder="Ajouter une note commerciale…" rows={3} className="ao-input flex-1 resize-none px-3 py-2 text-sm" /><button className="ao-btn-primary self-end px-4 py-2 text-xs font-black">Ajouter</button></form><div className="mt-4 space-y-2">{notes.map((note) => <article key={note.id} className="rounded-lg bg-[#FAF9F7] p-3"><p className="whitespace-pre-wrap text-sm leading-6">{note.content}</p><div className="mt-2 flex justify-between text-[10px] font-semibold text-[#8B7AA8]"><span>{formatDateTime(note.created_at)} · {note.created_by ? "louisdacre@gmail.com" : "Système"}</span><span><button onClick={() => void editNote(note)} className="mr-2">Modifier</button><button onClick={() => void deleteNote(note.id)} className="text-red-600">Supprimer</button></span></div></article>)}{!notes.length ? <Empty text="Aucune note." /> : null}</div></Section> : null}

      {tab === "tasks" ? <Section title="Tâches" action={<button onClick={() => setShowTask(!showTask)} className="text-xs font-black text-[#4C1D95]">+ Ajouter une tâche</button>}>{showTask ? <form onSubmit={addTask} className="mb-4 grid gap-3 rounded-lg bg-[#FAF9F7] p-3 sm:grid-cols-2"><label className="ao-label">Type<select name="type" className="ao-select h-9 px-2"><option>Appel</option><option>Email</option></select></label><label className="ao-label">Date<input required type="date" name="due_date" className="ao-input h-9 px-2" /></label><label className="ao-label">Heure facultative<input type="time" name="due_time" className="ao-input h-9 px-2" /></label><label className="ao-label sm:col-span-2">Description facultative<textarea name="description" className="ao-input px-2 py-2" /></label><button className="ao-btn-primary px-3 py-2 text-xs font-black sm:col-span-2">Créer la tâche</button></form> : null}<div className="space-y-2">{tasks.map((task) => <div key={task.id} className={`flex items-start gap-3 rounded-lg border p-3 ${task.completed ? "border-[#E8E4DB] bg-[#FAF9F7] opacity-60" : "border-[#E8E4DB]"}`}><input type="checkbox" checked={task.completed} onChange={(event) => void updateTask(task.id, { completed: event.target.checked })} className="mt-1" /><div className="min-w-0 flex-1"><div className={task.completed ? "text-sm font-black line-through" : "text-sm font-black"}>{task.title}</div><div className="mt-1 text-[11px] font-semibold text-[#6B617F]">{new Date(`${task.due_date}T12:00:00`).toLocaleDateString("fr-FR")}{task.due_time ? ` à ${task.due_time.slice(0,5)}` : " · Sans horaire"}{task.completed_at ? ` · Terminée ${formatDateTime(task.completed_at)}` : ""}</div>{task.description ? <p className="mt-1 text-xs text-[#6B617F]">{task.description}</p> : null}</div><button onClick={() => void editTask(task)} className="text-[10px] font-bold">Modifier</button><button onClick={() => void deleteTask(task.id)} className="text-[10px] font-bold text-red-600">Supprimer</button></div>)}{!tasks.length ? <Empty text="Aucune tâche." /> : null}</div></Section> : null}

      {tab === "events" ? <Section title="Événements" action={<button onClick={() => setShowEvent(!showEvent)} className="text-xs font-black text-[#4C1D95]">+ Ajouter un événement</button>}>{showEvent ? <form onSubmit={addEvent} className="mb-4 grid gap-3 rounded-lg bg-[#FAF9F7] p-3 sm:grid-cols-2"><label className="ao-label">Type<select name="type" value={eventType} onChange={(e) => setEventType(e.target.value as typeof eventType)} className="ao-select h-9 px-2">{CRM_EVENT_TYPES.map((type) => <option key={type}>{type}</option>)}</select></label><label className="ao-label">Date<input required type="date" name="event_date" className="ao-input h-9 px-2" /></label><label className="ao-label">Heure {eventType === "Appel effectué" ? "facultative" : ""}<input required={eventType !== "Appel effectué"} type="time" name="event_time" className="ao-input h-9 px-2" /></label>{eventType === "Appel effectué" ? <label className="ao-label">Résultat<select name="call_result" className="ao-select h-9 px-2"><option value="">Choisir</option>{CRM_CALL_RESULTS.map((result) => <option key={result}>{result}</option>)}</select></label> : <><label className="ao-label">Durée facultative<input type="number" min="5" name="duration_minutes" placeholder="30" className="ao-input h-9 px-2" /></label><label className="ao-label">Résultat facultatif<input name="result" className="ao-input h-9 px-2" /></label></>}<label className="ao-label sm:col-span-2">Notes<textarea name="notes" className="ao-input px-2 py-2" /></label><button className="ao-btn-primary px-3 py-2 text-xs font-black sm:col-span-2">Enregistrer l’événement</button></form> : null}<div className="space-y-2">{events.map((item) => <div key={item.id} className={`flex items-start gap-3 rounded-lg border p-3 ${['R1','R2','R3'].includes(item.type) ? "border-fuchsia-300 bg-gradient-to-r from-violet-50 to-fuchsia-50 shadow-sm" : "border-[#E8E4DB]"}`}><span className={`mt-1 h-3 w-3 rounded-full ${['R1','R2','R3'].includes(item.type) ? "bg-fuchsia-500 ring-4 ring-fuchsia-100" : "bg-[#A855F7]"}`} /><div className="min-w-0 flex-1"><div className="text-sm font-black">{item.title}</div><div className="mt-1 text-[11px] font-semibold text-[#6B617F]">{new Date(`${item.event_date}T12:00:00`).toLocaleDateString("fr-FR")}{item.event_time ? ` à ${item.event_time.slice(0,5)}` : ""}{item.duration_minutes ? ` · ${item.duration_minutes} min` : ""}</div>{item.call_result || item.result ? <div className="mt-1 text-xs font-black text-[#7C3AED]">{item.call_result ?? item.result}</div> : null}{item.notes ? <p className="mt-1 text-xs text-[#6B617F]">{item.notes}</p> : null}</div><button onClick={() => void editEvent(item)} className="text-[10px] font-bold">Modifier</button><button onClick={() => void deleteEvent(item.id)} className="text-[10px] font-bold text-red-600">Supprimer</button></div>)}{!events.length ? <Empty text="Aucun événement." /> : null}</div></Section> : null}

      {tab === "opportunities" ? <Section title="Opportunités" action={<button onClick={() => setShowOpportunity(!showOpportunity)} className="text-xs font-black text-[#4C1D95]">+ Créer une opportunité</button>}>{showOpportunity ? <form onSubmit={addOpportunity} className="mb-4 grid gap-3 rounded-lg bg-[#FAF9F7] p-3 sm:grid-cols-2"><label className="ao-label sm:col-span-2">Nom<input name="name" defaultValue={`AtriumOne - ${lead.name}`} className="ao-input h-9 px-2" /></label><label className="ao-label">MRR (€ / mois)<input required min="0" step="0.01" type="number" name="mrr" value={opportunityMrr || ""} onChange={(e) => setOpportunityMrr(Number(e.target.value))} className="ao-input h-9 px-2" /></label><label className="ao-label">ARR calculé<input readOnly value={`${calculateArr(opportunityMrr).toLocaleString("fr-FR")} € / an`} className="ao-input h-9 bg-gray-50 px-2" /></label><label className="ao-label">Statut<select name="status" className="ao-select h-9 px-2">{CRM_OPPORTUNITY_STATUSES.filter((status) => !['Gagnée','Perdue'].includes(status)).map((status) => <option key={status}>{status}</option>)}</select></label><label className="ao-label sm:col-span-2">Notes<textarea name="notes" className="ao-input px-2 py-2" /></label><button className="ao-btn-primary px-3 py-2 text-xs font-black sm:col-span-2">Créer l’opportunité</button></form> : null}<div className="grid gap-3 lg:grid-cols-2">{opportunities.map((item) => <article key={item.id} className={`rounded-xl border p-4 ${item.status === "Gagnée" ? "border-emerald-300 bg-emerald-50" : item.status === "Perdue" ? "border-red-200 bg-red-50" : "border-[#E8E4DB]"}`}><div className="flex items-start justify-between gap-3"><div><h3 className="text-sm font-black">{item.name}</h3><div className="mt-1 text-xl font-black text-[#4C1D95]">{Number(item.mrr).toLocaleString("fr-FR")} € <span className="text-xs text-[#8B7AA8]">MRR</span></div><div className="text-xs font-bold text-[#6B617F]">{Number(item.arr).toLocaleString("fr-FR")} € ARR</div></div><span className="rounded-full bg-white px-2 py-1 text-[10px] font-black">{item.status}</span></div>{item.notes ? <p className="mt-3 text-xs text-[#6B617F]">{item.notes}</p> : null}<div className="mt-4 flex flex-wrap gap-2">{!['Gagnée','Perdue'].includes(item.status) ? <><select value={item.status} onChange={(e) => void updateOpportunity(item, e.target.value)} className="ao-select h-8 px-2 text-[11px] font-black">{CRM_OPPORTUNITY_STATUSES.filter((status) => !['Gagnée','Perdue'].includes(status)).map((status) => <option key={status}>{status}</option>)}</select><button onClick={() => void updateOpportunity(item, "Gagnée")} className="rounded-lg bg-emerald-600 px-3 py-2 text-[11px] font-black text-white">Affaire closée</button><button onClick={() => void updateOpportunity(item, "Perdue")} className="px-2 text-[11px] font-black text-red-600">Perdue</button></> : null}<button onClick={() => void deleteOpportunity(item.id)} className="ml-auto text-[10px] font-bold text-red-600">Supprimer</button></div></article>)}{!opportunities.length ? <Empty text="Aucune opportunité." /> : null}</div></Section> : null}

      {tab === "access" ? <div className="grid gap-4 lg:grid-cols-2"><Section title="Compte AtriumOne"><div className="space-y-3"><Fact label="État" value={!lead.business_id ? "Aucun compte" : onboardingStatus === "suspended" ? "Suspendu" : accountEnabled ? "Actif" : "En attente d’onboarding"} /><Fact label="Email du compte" value={initial.account?.email} /><Fact label="Date d’inscription" value={formatDateTime(initial.account?.createdAt)} /><Fact label="Dernière connexion" value={formatDateTime(initial.account?.lastSignInAt)} /></div>{lead.business_id ? <div className="mt-4 border-t border-[#E8E4DB] pt-4"><label className="flex items-center justify-between gap-3 text-sm font-black"><span>Activer AtriumOne</span><button type="button" role="switch" aria-checked={accountEnabled} onClick={() => setAccountEnabled(!accountEnabled)} className={`ao-toggle ${accountEnabled ? "ao-toggle-on" : "ao-toggle-off"}`}><span className={`ao-toggle-thumb ${accountEnabled ? "ao-toggle-thumb-on" : "ao-toggle-thumb-off"}`} /></button></label><label className="ao-label mt-3">Statut<select value={onboardingStatus} onChange={(e) => setOnboardingStatus(e.target.value as BusinessAccess["onboarding_status"])} className="ao-select h-9 px-2"><option value="pending">En attente d’onboarding</option><option value="active">Actif</option><option value="suspended">Suspendu</option></select></label><div className="mt-4 text-xs font-black">Modules activés</div><div className="mt-2 grid gap-2 sm:grid-cols-2">{CRM_MODULES.map((key) => <label key={key} className={`flex items-center gap-2 text-xs font-semibold ${!accountEnabled ? "text-[#8B7AA8]" : ""}`}><input type="checkbox" disabled={!accountEnabled} checked={modules[key]} onChange={(e) => setModules({ ...modules, [key]: e.target.checked })} />{MODULE_LABELS[key]}</label>)}</div><button onClick={() => void saveAccess()} className="ao-btn-primary mt-4 w-full px-3 py-2 text-xs font-black">Enregistrer les accès</button></div> : null}</Section>
        {!lead.business_id ? <Section title="Comptes AtriumOne potentiels">{initial.candidates.map((candidate) => <div key={candidate.businessId} className="mb-2 rounded-lg border border-[#E8E4DB] p-3"><div className="text-sm font-black">{candidate.businessName}</div><div className="text-xs text-[#6B617F]">{candidate.email ?? "Email inconnu"} · {candidate.reason}</div><button onClick={() => void saveAccess(candidate.businessId)} className="mt-2 text-xs font-black text-[#4C1D95]">Associer</button></div>)}{!initial.candidates.length ? <Empty text="Aucun compte potentiel détecté." /> : null}</Section> : null}</div> : null}
    </main>
  </div>;
}

function Section({ title, action, children }: { title: string; action?: React.ReactNode; children: React.ReactNode }) { return <section className="rounded-xl border border-[#E8E4DB] bg-white p-4 shadow-sm"><div className="mb-3 flex items-center justify-between gap-3"><h2 className="text-sm font-black">{title}</h2>{action}</div>{children}</section>; }
function Fact({ label, value, href }: { label: string; value?: string | null; href?: string }) { return <div><div className="text-[10px] font-black uppercase tracking-wide text-[#8B7AA8]">{label}</div>{href && value ? <a href={href} target={href.startsWith("http") ? "_blank" : undefined} rel="noreferrer" className="break-words text-sm font-bold text-[#4C1D95]">{value}</a> : <div className="break-words text-sm font-bold">{value || "—"}</div>}</div>; }
function Empty({ text }: { text: string }) { return <div className="rounded-lg border border-dashed border-[#D9D1C3] p-4 text-center text-xs font-semibold text-[#8B7AA8]">{text}</div>; }
function eventActivityType(type: string) { return type === "Appel effectué" ? "call_completed" : type === "R1" ? "r1_completed" : type === "R2" ? "r2_completed" : type === "R3" ? "r3_completed" : type === "Point de suivi" ? "followup_completed" : "event_created"; }
function Timeline({ activity }: { activity: Activity[] }) { return <div className="relative space-y-0 before:absolute before:bottom-2 before:left-[7px] before:top-2 before:w-px before:bg-[#E8E4DB]">{activity.map((item) => { const prominent = ['r1_completed','r2_completed','r3_completed','deal_won'].includes(item.type); return <div key={item.id} className={`relative flex gap-3 rounded-lg py-2 ${prominent ? "my-1 bg-gradient-to-r from-violet-50 to-fuchsia-50 px-2" : ""}`}><span className={`mt-1.5 h-[15px] w-[15px] shrink-0 rounded-full border-[4px] border-white ring-1 ${prominent ? "bg-fuchsia-500 ring-fuchsia-300" : "bg-[#A855F7] ring-[#D8B4FE]"}`} /><div><div className={`text-xs font-black ${prominent ? "text-[#6D28D9]" : ""}`}>{activityLabel(item)}</div><time className="mt-0.5 block text-[10px] font-semibold text-[#8B7AA8]">{formatDateTime(item.created_at)}</time></div></div>; })}{!activity.length ? <Empty text="L’activité apparaîtra ici." /> : null}</div>; }
function activityLabel(activity: Activity) { const labels: Record<string,string> = { lead_created: "Prospect ajouté au CRM", status_changed: `Statut → ${String(activity.metadata.to ?? "mis à jour")}`, search_source_added: `Recherche source ajoutée · ${String(activity.metadata.city ?? "")} ${String(activity.metadata.business_type ?? "")}`, note_added: "Note ajoutée", note_updated: "Note modifiée", note_deleted: "Note supprimée", task_created: `Tâche créée · ${String(activity.metadata.title ?? "")}`, task_completed: `Tâche terminée · ${String(activity.metadata.title ?? "")}`, task_updated: "Tâche mise à jour", task_deleted: "Tâche supprimée", call_completed: `Appel effectué${activity.metadata.result ? ` · ${String(activity.metadata.result)}` : ""}`, r1_completed: "R1 réalisé", r2_completed: "R2 réalisé", r3_completed: "R3 réalisé", followup_completed: "Point de suivi réalisé", event_created: "Événement ajouté", event_updated: "Événement modifié", event_deleted: "Événement supprimé", opportunity_created: `Opportunité créée · ${String(activity.metadata.name ?? "")}`, opportunity_status_changed: `Opportunité → ${String(activity.metadata.to ?? "mise à jour")}`, deal_won: `Affaire gagnée · +${Number(activity.metadata.mrr ?? 0).toLocaleString("fr-FR")} € MRR · +${Number(activity.metadata.arr ?? 0).toLocaleString("fr-FR")} € ARR`, deal_lost: "Affaire perdue", prospect_became_client: "Prospect devenu client", account_associated: "Compte AtriumOne associé", account_activated: "Compte AtriumOne activé", account_disabled: "Compte AtriumOne désactivé" }; return labels[activity.type] ?? activity.type.replaceAll("_", " "); }
